# Copia a `.env` en esta carpeta (no lo subas a git). Expo carga EXPO_PUBLIC_* automáticamente.
# Debe ser la URL HTTPS del servicio api-server en Railway (Networking), NUNCA la URL/host del servicio Postgres.

EXPO_PUBLIC_API_URL=https://tu-api-en-railway.up.railway.app
